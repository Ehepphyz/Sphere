package com.sphere.components.editor;

/** Token classes the highlighter can colour. */
public enum TokenKind {
    DEFAULT,
    KEYWORD,
    TYPE,
    FUNCTION,
    NUMBER,
    STRING,
    CHARACTER,
    COMMENT,
    PREPROCESSOR,
    DECORATOR,
    OPERATOR
}
